Deliberately empty: vite.config.ts points device builds (MOBILE_BUILD / DESKTOP_BUILD)
at this folder as envDir so no .env file in the repository is loaded into an
Android APK or a Windows installer.
