# Approval, permission and authority discovery — 2026-09-09

## Method and scope

Repository-wide searches covered permission calls, legacy PIN rules, the central authorization action catalogue, transfer and shift state machines, Supabase migrations, Electron SQLite/outbox tables, approval UI, authentication, realtime polling and notification events. This report records existing business rules; it does not turn ordinary permission checks into approvals.

## Approval matrix

| Operation                                         | Current permission/rule                                                       |                          Limit | Approval supported / approver                                               | Offline behaviour                                                   | Audit / finding                                                                        |
| ------------------------------------------------- | ----------------------------------------------------------------------------- | -----------------------------: | --------------------------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Refund                                            | `can_process_refund`; `refund` gate                                           |         refund-day/value rules | PIN/request modes exist; configured role/user                               | policy may require online                                           | authorization log; direct refund permission remains separate                           |
| Void cart                                         | `void_cart`                                                                   |                           none | configured PIN/request                                                      | `online_only_void_cart` controls                                    | authorization log                                                                      |
| Void line / restricted removal                    | `void_line`                                                                   |                           none | configured PIN/request                                                      | `online_only_void_line` controls                                    | authorization log                                                                      |
| Reduce scanned quantity                           | `reduce_qty`                                                                  |                           none | configured PIN/request                                                      | branch offline policy                                               | authorization log                                                                      |
| Manual item/bill discount                         | `can_give_discount`; `manual_discount`                                        |       branch percentage/amount | configured PIN/request                                                      | branch offline policy                                               | authorization log                                                                      |
| Discount above cashier limit                      | `discount_over_limit`                                                         | cashier percent or cart amount | automatic central dialog; configured role/user and per-approver limit       | local PIN only when policy permits; otherwise ticket remains parked | request/log/activity event; **fixed missing value/limit context and approver ceiling** |
| Price override                                    | `price_override`                                                              |                           none | configured PIN/request                                                      | `online_only_price_override`                                        | authorization log                                                                      |
| Below-cost sale                                   | `below_cost_sale`                                                             |                  cost boundary | configured PIN/request                                                      | online unless explicitly supported                                  | authorization log                                                                      |
| Tax exemption                                     | `tax_exemption`                                                               |                           none | configured PIN/request                                                      | online unless explicitly supported                                  | authorization log                                                                      |
| Edit split tenders                                | `edit_tenders`                                                                |                           none | configured PIN/request                                                      | `online_only_edit_tenders`                                          | authorization log                                                                      |
| No-sale drawer                                    | `can_no_sale_open`; `no_sale_drawer`                                          |                           none | configured authorizer                                                       | branch offline policy                                               | drawer event + authorization log                                                       |
| Shift close                                       | `can_close_shift`; `shift_close`                                              |                           none | configured authorizer                                                       | branch offline policy                                               | shift reconciliation/log                                                               |
| Shift variance                                    | `can_shift_variance_approve`; `shift_close_variance`                          |            configured variance | explicit variance approval                                                  | local shift state survives interruption                             | reconciliation + activity                                                              |
| Stock adjustment                                  | `stock_adjustment`                                                            |                           none | deferrable central request                                                  | queued in Electron governance outbox                                | authorization log                                                                      |
| Delete product                                    | `can_add_new_product`; `delete_product`                                       |      references block deletion | deferrable central request                                                  | queued where safe                                                   | authorization log                                                                      |
| Edit posted stock                                 | `edit_posted_stock`                                                           |                           none | deferrable, record held atomically                                          | outbox/hold                                                         | authorization + immutable edit history                                                 |
| Edit received purchase                            | `edit_posted_purchase`                                                        |                           none | deferrable, record held atomically                                          | outbox/hold                                                         | authorization + edit history                                                           |
| Discard populated draft                           | `discard_draft`                                                               |                           none | deferrable central request                                                  | queued where safe                                                   | authorization log                                                                      |
| Terminal unpair                                   | admin or `terminal_unpair`                                                    |                           none | central gate                                                                | requires verifiable authority                                       | activation log                                                                         |
| Member points adjustment                          | `member_points_adjust`                                                        |                           none | central gate                                                                | queued where safe                                                   | authorization log                                                                      |
| Stock transfer                                    | `can_approve_transfer`; setting `requireTransferApproval`                     |            approved quantities | transfer state machine; eligible receiving/approving staff                  | local transfer rows sync; stock moves only at dispatch/receipt      | transfer history; intentionally remains domain workflow                                |
| Shift/transfer/settings/staff/role administration | named permissions (`can_access_pos_settings`, transfer and staff permissions) |                            n/a | direct permission only unless an action above explicitly defines escalation | blocked offline where server authority is required                  | ordinary permission/audit trails; no invented approval                                 |
| Terminal activation                               | authenticated admin activation flow                                           |                            n/a | explicit remote pairing approval                                            | cannot weaken to offline activation                                 | activation attempt log                                                                 |

## Findings and repairs

1. The shared framework already prevents self-approval, uses a pending-only compare-and-set decision, binds requests to ticket fingerprints, consumes grants once, parks in-progress tickets, polls/reconciles realtime state, and uses idempotent activity-event IDs.
2. Request permission and decision permission were conflated: any authenticated cashier could submit any action key, while queue visibility only checked approver role/user. Requester roles/users are now independently configured and server-enforced.
3. A valid approver PIN/session had no value ceiling. Configurable `role:<role>` and `user:<id>` limits are now checked both for terminal PIN authorization and remote decisions. A personal limit overrides a role limit.
4. Above-limit discount requests omitted requested amount, allowed limit, type, scope and transaction reference. The automatic dialog now presents a human explanation and sends this context to the queue/audit path.
5. The existing offline implementation deliberately refuses unverifiable PINs rather than trusting any cached PIN. Requests/actions remain parked and enter the SQLite sync queue; online-only policy produces a clear message. This security posture is retained.
6. Transfer approval remains its existing quantity/state-machine workflow rather than being duplicated in the generic queue. Ordinary settings, staff and role permission failures remain prohibited unless the existing authorization action catalogue explicitly supports escalation.

## Required invariants

- Direct permission and within-limit actions execute normally; exact threshold is inclusive.
- Exceeding a configured limit opens the shared authorization dialog and never mutates the cart first.
- Only configured requesters may queue; only configured active authenticated authority may decide; PIN validity alone is insufficient.
- A decision is pending-only, self-approval is refused, and a grant is ticket/action scoped and single-use.
- Reject/withdraw affects only the request. The underlying held sale remains intact.
- Approval state and activity/read state remain separate; deterministic event IDs prevent completed notifications from being recreated as new pending events.

## Continuation: notifications versus authoritative attention

The activity feed is an immutable event history with per-user view preferences; authorization requests, transfer states, and refused sync queue rows are separate source records. Previously, the header applied the notification cleared-ID set to approval requests too. That made a pending request disappear from the popover even though its database status remained pending.

The centre now derives **Needs attention** from source state only: pending visible authorization requests, transfers awaiting approval or arrival verification, and refused sync queue rows. Read/clear markers affect activity notifications only. Approval, rejection, withdrawal, expiry, transfer progression, or successful sync retry removes attention automatically on the next realtime/poll/store update; history remains in its existing source log.

## Continuation: approval-limit storage semantics

| Field                                                          | Storage                      | Meaning / compatibility                                                                                          | Scope and precedence                                              |
| -------------------------------------------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `threshold`                                                    | `authorization_actions`      | Existing direct/no-authorization threshold metadata; unchanged                                                   | branch rule overrides global rule                                 |
| POS `max_cashier_discount_percent`, `max_cart_discount_amount` | POS rules                    | Requester direct limits used by the register; unchanged                                                          | existing settings inheritance                                     |
| `authority_limits`                                             | `authorization_actions` JSON | **Legacy absolute approver maximum** introduced in the preceding migration; never reinterpreted                  | `user:<id>` overrides `role:<role>`; branch rule overrides global |
| `extra_authority`                                              | `authorization_actions` JSON | New relative allowance added to the captured requester direct limit; discount percentages mean percentage points | user, then role; branch, then global                              |
| `absolute_ceilings`                                            | `authorization_actions` JSON | Optional cap on the relative result                                                                              | user, then role; branch, then global                              |
| `requester_direct_limit`                                       | `authorization_requests`     | Immutable-at-request context used to reconstruct and validate the exact decision                                 | request-specific                                                  |
| `value_unit`                                                   | `authorization_requests`     | `percent`, `currency`, `quantity`, or generic `number`                                                           | request-specific                                                  |

When a matching `extra_authority` exists, effective maximum is `min(direct limit + extra allowance, optional absolute ceiling)`. When it does not, a matching legacy `authority_limits` value remains an absolute maximum. When neither is configured, the existing role/user authorization remains unlimited. Invalid or negative input is refused. A denied low-authority attempt logs the calculation but leaves the one request pending for a higher authority.

## SQL deployment

The additive cloud migration is `supabase/migrations/20260909130000_relative_approval_authority.sql`. Apply it to Supabase before deploying application code. The earlier `20260909090000_approval_authority_limits.sql` remains required only on a database where that preceding change has not already been applied. Windows SQL Server upgrades are generated from `database/schema.sql` and run through the existing POS Schema Manager/startup upgrade; the generated `db/offline/pos-offline-sqlserver.sql` is for a technician provisioning/upgrading SQL Server, not Supabase. Electron SQLite additions are applied by the existing additive local-schema upgrader; reset scripts must never be used.
