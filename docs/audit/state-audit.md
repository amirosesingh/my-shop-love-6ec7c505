# POS state audit — 1 September 2026

Every line below was checked against the code and the live central database on
the day of writing. Nothing is carried over from earlier completion messages.

Evidence style: `file:line` for code, `information_schema` queries for the
cloud, `database/schema.sql` for the till's own database.

---

## 1. Headline numbers

| Measure | Value | Source |
| --- | --- | --- |
| Tables in the central database | 63 tables + 3 reporting views | `information_schema.tables` |
| Tables the till keeps locally | 37 | `TABLES`, `electron/db/repo.cjs:11` |
| Tables the till pushes to the cloud | 36 | `PUSH_TABLES`, `repo.cjs:184` |
| Tables pulled down as reference data | 8 | `CATALOGUE_TABLES`, `repo.cjs:60` |
| Tables pulled down branch-scoped | 5 | `SCOPED_PULL_TABLES`, `repo.cjs:79` |
| Tables restorable onto a wiped till | 22 | `RESTORE_TABLES`, `repo.cjs:99` |
| Features declared in the registry | 10, covering 37 operations | `src/lib/feature-schema.ts:91+` |
| Business screens in the app | 47 routes outside Settings | `src/routes/` |
| Settings pages | 42 | `src/routes/settings.*.tsx` |

The one local table with no cloud counterpart is `pos_settings`; it is pulled
down, never pushed, which is correct — head office owns it.

---

## 2. Classification by area

| Area | Status | Evidence | Action |
| --- | --- | --- | --- |
| Feature registry | COMPLETE | `feature-schema.ts` declares columns, sync direction, restore requirement and security class per operation, and probes them against the live database | Reuse; extend coverage in §3 |
| Sync coverage matrix | COMPLETE | `sync-coverage.ts` compares declared intent against the till's real push/pull/restore lists; surfaced in Logic Health; regenerated by `scripts/sync-coverage.cjs`; currently reports 27 tables, 0 gaps | Reuse; extend with recovery verdicts |
| Sales sync | COMPLETE | `sales`, `sale_items` pushed and restorable (`repo.cjs:101-102`); duplicate protection on `client_transaction_id` | None |
| Payments sync | COMPLETE | `payment_transactions` pushed and restorable (`repo.cjs:103`) | None |
| Shifts | COMPLETE | shifts, sessions, cash counts, close events, reconciliations, variance alerts all pushed and restorable (`repo.cjs:99-107`) | None |
| Shift closing controls | COMPLETE | free-text mandatory reason, one-way `reason → count → review → done` machine, server-side expected cash and variance, recount preserved separately (`src/components/pos/ShiftCloseDialog.tsx:43-300`, `shift_close_start` / `shift_cash_count_submit` / `shift_reconcile_now` / `shift_recount_submit`) | Verify only; do not rewrite |
| Booking collect / cancel / refund | COMPLETE | server routines `booking_collect`, `booking_cancel`, `booking_refund`, `bookings_block_unpaid_collection`, `booking_payment_within_total`; `cancel_reason` stored (`src/lib/bookings-db.ts:61`) | None |
| Bookings recovery | COMPLETE | `bookings` + `booking_payments` pulled branch-scoped every cycle (`repo.cjs:83-84`) | None |
| Inventory | PARTIAL | products pulled; movements captured in `stock_adjustments` and `item_activity_logs`, both restorable — but purchase receipts and transfer receipts are not written as movement rows, so movement history is incomplete | Fix in Stage 2 |
| Purchasing | COMPLETE | `purchase_orders` + `purchase_order_items` pushed and restorable (`repo.cjs:114-115`) | None |
| Stock transfers | COMPLETE | pushed and branch-scoped pulled both ways (`repo.cjs:81-82`) | None |
| **Stock requests** | **PENDING** | zero matches for `stock_request` across the whole repository and zero tables in the central database | Build in Stage 3 |
| **Status history** | **PENDING** | no history table anywhere; `bookings` keeps only `status`, `job_status`, `job_status_by`, `job_status_at` — the last change only, overwritten by the next | Build in Stage 2 |
| **Business event history** | **PARTIAL** | `activity_events` exists, is written offline-safe and restorable, but has no entity id, no previous state and no new state — only `title`, `message` and a free JSON blob (`database/schema.sql:592`) | Extend in Stage 2 |
| Governance trail | COMPLETE | `record_edits`, `authorization_requests`, `authorization_log`, `member_verifications` all pushed and restorable; offline parking via `src/lib/governance-offline.ts` | None |
| **Deletion / tombstones** | **PENDING** | no `deleted_at` column anywhere in the central database; only `archived_at` on `products` and `stores`. A record deleted centrally while a till is offline is never removed from that till | Build in Stage 4 |
| Stable identifiers | COMPLETE | UUID primary keys minted at the till; `row_version` present on 30 central tables; `client_transaction_id` unique index prevents duplicate sales after a wipe | Spot-check in Stage 6 |
| Conflict management | PARTIAL | `row_version` plus `skip_stale_update` gives version-aware writes, and finalised sales, shift records and authorization logs are protected by immutability triggers — but the rules are not written down anywhere | Document in Stage 4 |
| Incremental sync | PARTIAL | delta pull on `updated_at` with a per-table watermark (`electron/sync/worker.cjs:71-104`), retry and resume present; restore pages, routine pull does not | Add paging in Stage 4 |
| Local outbox | COMPLETE | every write lands in the local database first with `is_synced` / `sync_status`, and the sync worker drains it; governance falls back to parking when the cloud refuses | None |
| Sync health view | PARTIAL | connection, last sync, pending, failures and coverage exist across Logic Health and the Sync panel — but there is no per-feature recovery verdict | Build in Stage 5 |
| Settings organisation | COMPLETE | One registry: the card catalogue names every area, the sub-tab groups take their wording from it, and Logic health lists any page with no card | None |
| Terminal management | COMPLETE | `terminal_tokens`, `terminal_commands`, `branch_telemetry` with heartbeat and kill switch | None |
| Backups | NOT VERIFIABLE FROM CODE | the central database is managed; backup policy is outside the repository | Confirm with the platform, not in code |

---

## 3. Recovery verdict per feature

The question for each: *if this till's database is deleted tonight, can a
replacement rebuild it from the cloud?*

This is no longer maintained by hand. The verdicts are derived from the feature
registry and the till's own sync lists, written to
[`recovery.md`](./recovery.md) by `bun scripts/sync-coverage.cjs`, and shown in
Logic Health under "If this till had to be rebuilt".


## 4. Field-level sync classification

Already enforced and correct:

- **Cloud authoritative** — products, categories, barcodes, units, suppliers,
  promotions, stores, membership tiers, settings. Pulled, never pushed.
- **Branch synchronised** — sales, payments, shifts, purchasing, transfers,
  adjustments, bookings, governance. Pushed, restorable.
- **Global** — members, shared across every branch.
- **Terminal specific** — activation state, telemetry. Never mirrored down.
- **Central only** — 26 tables listed with a stated reason in
  `sync-coverage.ts:38`.

Secrets are correctly excluded: `secure_settings` is central-only, PIN hashes
live in `app_users` which is never mirrored to a till, activation tokens are an
online-only exchange, and local credentials are sealed with OS hardware
encryption rather than synced.

No secret material was found in any pushed, pulled or restored table.

---

## 5. Duplicate and obsolete code candidates

Nothing here will be deleted without your say-so.

| Path | What it is | Why it looks obsolete | Risk of removal |
| --- | --- | --- | --- |
| `public.cashiers` (central) | Legacy staff table, 11 columns | Superseded by `app_users` + `user_roles`; kept only for `legacy_cashiers_for_migration` and `staff_account_adopt_legacy` | Medium — confirm no branch still adopts legacy accounts |
| `src/utils/syncResolver.ts` | Older conflict helper | The worker resolves conflicts itself with `row_version`; needs a caller check | Low |
| `public.audit_logs` vs `system_audit_logs` vs `activity_events` | Three overlapping trails | Three tables record overlapping activity with different scopes | High — do not merge without a plan |
| `settings_scoped` / `settings_overrides` / `settings_locks` / `pos_store_settings` | Four settings mechanisms | Overlapping resolution paths | High — behaviour depends on all four |

Recommendation: leave all four alone for now. They are documentation debt, not
active faults, and merging trails mid-audit risks losing history.

---

## 6. Stage 2 — delivered

- `entity_status_history` created centrally: append-only (updates and deletes
  are refused by trigger), branch-scoped read policy, staff-append policy,
  duplicate-proof on `client_event_id`.
- Mirrored on the till in `database/schema.sql`, added to the push list and to
  the restore list, so a transition recorded with no connection survives both
  the outage and a terminal rebuild.
- `activity_events` gained `previous_state` and `new_state`; `audit_logs`
  gained `store_id`, which makes the till's audit trail restorable — it is now
  on the restore list, closing one of the six recovery failures.
- Transitions are now recorded at every state change the till owns: shift
  opened and closed, booking cancelled and collected, job card moved between
  received / strung / ready / collected, transfer approved, received, rejected
  and cancelled.
- A read-only History dialog on each booking shows the timeline.

## 7. What happens next

Stage 3 — stock requests, end to end.
Stage 4 — tombstones, paging, written conflict rules.
Stage 5 — per-feature recovery verdicts in Logic Health.
Stage 6 — the wipe-and-restore test. **Delivered:** a rebuild check and a
guarded, reversible drill on the till; see
[`restore-test.md`](./restore-test.md).
Stage 7 — settings restructure. **Delivered:** groups derived from the card
catalogue, document numbering given a card, bookings given their own category,
and a coverage check in Logic health.
Stage 8 — inventory ledger, security sweep and final report. **Delivered:**

- **Inventory movement is now complete.** `item_activity_logs` used to record
  sales only. Goods received now write one movement row per line
  (`receivingActivityRows` in `src/lib/pos-db.ts`, committed atomically with the
  invoice and keyed off the invoice so a resumed draft or a correction rewrites
  the same rows instead of doubling them). Branch transfers write
  `transfer_out` on dispatch and `transfer_in` on receive inside the
  `stock_transfer_dispatch` / `stock_transfer_receive` functions, with
  `stock_before` and `stock_after` captured in the same statement as the stock
  change.
- The item history drawer reads the ledger directly, so an item now shows stock
  arriving as well as leaving, with plain wording for each movement kind.
- **Security sweep.** Public API routes all authenticate before touching a
  record (till token, signed body, or relay caller verification); the relay
  allow-list scopes `item_activity_logs` by `store_id` like every other
  branch-owned table; barcode lookups are no longer readable by signed-out
  visitors. The remaining linter warnings are the `SECURITY DEFINER` functions
  the permission model is built on, and the realtime feeds, which stay
  RLS-gated.

